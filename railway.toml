[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:5000 --workers 2 --timeout 120 app:app"
restartPolicyType = "on_failure"